# Core (installed via TinyZero's pip install -e .)
# torch>=2.4.0
# vllm==0.6.3
# ray
# flash-attn

# Additional dependencies for this project
sentence-transformers>=2.2.0
datasets>=2.14.0
pandas>=2.0.0
wandb
hydra-core>=1.3.0
omegaconf>=2.3.0
